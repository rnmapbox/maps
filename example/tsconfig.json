 {
  "extends": "../tsconfig",
  "compilerOptions": {
    // Avoid expo-cli auto-generating a tsconfig
    "allowJs": true,
    "checkJs": false,
  },
  "include": [
    "src/**/*.ts",
    "global.d.ts"
  ]
}
